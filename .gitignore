# Dependency folders
node_modules/

# Environment variables
.env 👈 สำคัญที่สุด! เพื่อซ่อน MONGO_URI (รหัสผ่าน)
server/.env

# Build artifacts (ถ้าสร้าง frontend แล้ว)
client/build
client/dist